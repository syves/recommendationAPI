import org.scalatest._

import scalaz.concurrent.{Task, Strategy}
import scalaz._, Scalaz._

import ParseJson._
import RecommendationService._
import RecommendationUtils._

class recommendationAPISpec extends FlatSpec with Matchers {
/*
	"eitherCashFlow" should "return a msg that describes the parse error" in {
		val actual = eitherCashFlow(malformed1)
    	val expected = Left("JSON terminates unexpectedly.")
    	assert(actual == expected)
	}
*/
}
